version: '3.8'

services:
  web:
    image: nginx:alpine
    ports:
      - "8080:80"
    command: [nginx-debug, '-g', 'daemon off;']
  
  db:
    image: postgres:15-alpine
    environment:
      POSTGRES_PASSWORD: supersecretpassword
